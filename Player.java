package com.company;

public class Player extends Entity{
    public Player(String name,  String role, double hp, double dmg, double armor, double critChance) {
        super(name, role, hp, dmg, armor, critChance);
    }
}
